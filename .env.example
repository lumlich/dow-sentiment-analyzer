# dow-sentiment-analyzer — .env.example
# Copy this file to `.env` and fill in values as needed.

# --- Relevance gate ---
# Path to the TOML with anchors/blockers/etc.
RELEVANCE_CONFIG_PATH=config/relevance.toml
# Gate threshold in [0.0, 1.0]; texts with score < threshold are neutralized.
RELEVANCE_THRESHOLD=0.50
# Optional (dev only):
# RELEVANCE_HOT_RELOAD=1
# RELEVANCE_DEV_LOG=1

# --- AI (optional) ---
# Set these only if you enable AI in config/ai.json (e.g., { "enabled": true, "api_key": "ENV" }):
# Provider API key (required when ai.json.api_key == "ENV")
OPENAI_API_KEY=
# Toggle AI usage at runtime (1=on, 0=off)
AI_ENABLED=1
# Daily cap for AI calls
AI_DAILY_LIMIT=50
# Optional knobs (use only if your build reads them)
# AI_SCORE_BAND=0.08
# AI_ONLY_TOP_SOURCES=1

# --- Shuttle (local dev) ---
# `cargo shuttle run` sets this automatically; you usually don't need to set it manually.
# SHUTTLE_ENV=local
# CORS
ALLOWED_ORIGINS=http://localhost:5173